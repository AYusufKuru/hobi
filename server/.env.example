PORT=3001
# Comma-separated frontend origins (Vercel URL, localhost, etc.)
# CORS_ORIGIN=https://hobi.vercel.app,http://localhost:5173
# sqlite (default, zero-setup) | postgres
DB_TYPE=sqlite
# When DB_TYPE=postgres:
DB_HOST=localhost
DB_PORT=5432
DB_USER=govorbit
DB_PASSWORD=govorbit
DB_NAME=govorbit
